#include <cstdlib> 
#include <iostream> 
#include <stdio.h> 
#include <conio.h> 
using namespace std;
int main(int argc, char *argv[])
{
	int miesi¹c;
	cost <<"podaj-miesi¹c urodzenia"
	cin >> miesi¹c;
	for(int i = miesiac+ (miesiac% 2) ; i <= miesiac +30;i+2 ) 
 system("PAUSE");
 return EXIT_SUCCESS;
}

